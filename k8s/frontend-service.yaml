apiVersion: v1
kind: Service
metadata:
  name: learning-engine-frontend
  namespace: learning-engine
spec:
  type: NodePort
  selector:
    app: learning-engine-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30180
