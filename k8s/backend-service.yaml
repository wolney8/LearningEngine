apiVersion: v1
kind: Service
metadata:
  name: learning-engine-backend
  namespace: learning-engine
spec:
  type: ClusterIP
  selector:
    app: learning-engine-backend
  ports:
    - name: http
      port: 8000
      targetPort: http
